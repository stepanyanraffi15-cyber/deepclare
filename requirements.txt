# Pinned. See README for the tested install steps.
pydantic==2.12.3
pydantic-settings==2.11.0
python-dotenv==1.2.1
httpx==0.28.1
qdrant-client==1.16.1

# Page rendering. pypdfium2 ships the PDFium engine in the wheel — no system package to
# install — and renders at an arbitrary scale, which is what a fixed-DPI raster needs.
# Pillow encodes the rendered bitmap to PNG.
pypdfium2==5.12.1
pillow==12.3.0

# Tests. Needed only to run `tests/`; nothing under `src/` imports it. It is pinned here
# because this is the one pinned dependency list, but pyproject feeds this whole file to
# the package's runtime dependencies, so it installs in production too. Splitting a
# dev-only list out is a packaging decision that has not been taken.
pytest==9.1.1
